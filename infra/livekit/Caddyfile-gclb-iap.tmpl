# Caddy 站点片段 —— 单域名 + GCLB/IAP 拓扑（2026-09-12 起的现行形态，默认用这份）
#
# 由 scripts/install-livekit.sh --caddy-mode gclb-iap 渲染，落到
# /etc/caddy/sites/closecrab-livekit.caddy。**不是** /etc/caddy/Caddyfile ——
# 那份是共用的，同一台机器上通常还挂着别的站点，整份覆盖会把它们全删掉。
#
# 同目录下的 `Caddyfile.tmpl` 是「双域名 + Caddy 自己签证书 + 443 直接对公网」
# 的旧形态（--caddy-mode direct），两者不兼容：这份让 IAP 挡在前面，那份绕过
# IAP。别把两份混着用，脚本也只会装其中一份。
#
# 占位符：
#   __FRONTEND_DOMAIN__   对外域名（页面和信令共用，见下）
#   __FRONTEND_UPSTREAM__ 前端回源地址，前端不在本机时写它的内网 IP:3000
#   __SFU_UPSTREAM__      SFU 回源地址，SFU 在本机就是 127.0.0.1:7880
#
# 拓扑：
#   浏览器 ──HTTPS──► GCLB + IAP ──HTTP:80──► 本机 Caddy ──┬─► __SFU_UPSTREAM__      (SFU 信令)
#                                                          └─► __FRONTEND_UPSTREAM__ (Next.js)
#   媒体  ──UDP 50000-60000──────────────────────────────► 本机外网 IP（**不过 LB**）
#
# 为什么媒体不能也走 LB：ICE candidate 里带的是裸 IP:port，浏览器直接往那个
# 地址发 UDP。HTTPS LB 不转发 UDP，要想藏在 LB 后面得另外架 TURN。

# ── 为什么 signaling 和前端必须是**同一个域名** ─────────────────────
# IAP 的登录 cookie 是**按主机名**发的。浏览器只在导航过的那个域名上拿到
# cookie。独立的 signaling 域名从来没有人用浏览器导航过去 → 它没有 cookie →
# WebSocket 握手被 IAP 弹 302 到登录页 → 浏览器**不会跟随重定向做 WS 升级**，
# 直接失败。同源就不存在这个问题。
# ⇒ 「想让 signaling 也受 IAP 保护」和「用独立 signaling 域名」是互斥的。
#
# 站点地址必须显式写 `http://`：TLS 在 GCLB 上就终止了，到这里是明文 80。
# 不写的话 Caddy 会启用 automatic HTTPS，自己去签证书并把 80 重定向到 443。
#
# 也**不要**把这几条塞进一个泛 `:80 {}` 块。独立 site block 更具体、
# Caddy 优先匹配，跟同机上其他服务互不干扰。
http://__FRONTEND_DOMAIN__ {
	header -Server
	header X-Content-Type-Options nosniff

	# ── X-CC-Entry：这一组 header_up 是整个 /native 方案的信任边界 ──────
	# Caddy 是**唯一**入口（GCLB 只往这里回源），所以「请求从哪条路进来的」
	# 这件事只有它说了算。它在 /native/* 上**覆盖**写入 X-CC-Entry: native，
	# 在浏览器那两条路上**显式 unset**。
	#
	# 两边都要写，缺一不可：
	#   · 只写 set 不写 unset ⇒ 浏览器路上自己带一个 X-CC-Entry: native 的请求
	#     会**同时**拿到 IAP 放行和 native 分支 —— 前端就会跳过验签。
	#   · header_up 是覆盖不是追加，所以客户端自带的同名头在 /native/* 上也会被
	#     盖掉，伪造不了。
	# 前端消费方：frontend/lib/native-auth.ts 的 isNativeEntry()。

	# /lk/* → SFU 信令（浏览器走这条，在 IAP 后面）。handle_path 会剥掉 /lk 前缀，
	# 所以浏览器请求的 /lk/rtc/v1 到 livekit-server 眼里就是 /rtc/v1。
	# 两边 SDK 都保留路径前缀（已读源码确认：
	#   JS   livekit-client createV0RtcUrl → appendUrlPath(urlObj,'rtc')
	#   Py   agents/worker.py:1102 base = scheme://netloc+path，再 urljoin 'agent'）
	handle_path /lk/* {
		reverse_proxy __SFU_UPSTREAM__ {
			header_up -X-CC-Entry
		}
	}

	# /native/lk/* → 同一个 SFU，但走非 IAP 后端。原生 app 没有浏览器的登录
	# cookie，WS 升级会被 IAP 弹 302，而客户端不会跟着重定向做握手，直接失败。
	# 信令本身不靠 IAP 保护 —— 进房间要的是那张 15 分钟的 JWT，
	# 而 JWT 只能从下面那条验过签的 /native/api/token 换到。
	# 这条必须排在 /native/* 前面：Caddy 的 handle_path 按书写顺序匹配，
	# 反过来写的话信令会被当成普通 API 转给前端。
	handle_path /native/lk/* {
		reverse_proxy __SFU_UPSTREAM__ {
			header_up X-CC-Entry native
		}
	}

	# 管理台**不开给 native**。/admin 和 /api/admin/* 自己没有任何鉴权 ——
	# 它们一直是靠 IAP 挡着的，而 /native/* 恰恰是绕开 IAP 的那条路。
	# 下面那条 handle_path 会把 /native 前缀剥掉，所以不挡的话
	# `/native/api/admin/rooms` 就是一个公网可达、不用签名的
	# deleteRoom / removeParticipant 接口。2026-09-13 真这么裸奔过约一小时。
	# 这两条必须排在通配那条前面 —— handle 系列按书写顺序匹配。
	handle /native/admin* {
		respond "not found" 404
	}

	handle /native/api/admin/* {
		respond "not found" 404
	}

	# /native/* → 前端，但不走 IAP，改由 HMAC 验签把门（CC_NATIVE_SECRET）。
	# 前缀被剥掉，所以 app 请求的 /native/api/token 到 Next.js 眼里就是 /api/token
	# —— 服务端只有一套路由，两条入口共用。
	handle_path /native/* {
		reverse_proxy __FRONTEND_UPSTREAM__ {
			header_up X-CC-Entry native
		}
	}

	# 其余全给前端（浏览器路，IAP 已经在上游验过身）
	handle {
		reverse_proxy __FRONTEND_UPSTREAM__ {
			header_up -X-CC-Entry
		}
	}
}

# ── 配套的 GCLB 侧注意事项（不是 Caddy 配置，但漏了就白做）───────────
# 1. url-map 的 IAP 豁免前缀会**连带**豁免这个域名下的同名路径（host 规则是 `*`
#    时尤其如此）。豁免清单里绝不能出现 /api/*、/admin*、/lk/*。
#
#    **`/native/*` 是这条规则的唯一例外，而且不是网开一面。** 原生 app 没有
#    浏览器 cookie，IAP 对它是「永远进不去」而不是「多一道门」，所以那条路的
#    鉴权换成了另一套，不是取消了：
#      · `/native/api/*` —— 每个请求带 `HMAC-SHA256(CC_NATIVE_SECRET,
#        "<scope>:<unix 秒>")`，±300 秒时窗。scope 对 /api/token 是房间名、
#        对 /api/rooms 是字面量 `rooms`，所以一张签名只能用在它申请的那个房间上，
#        抓到一次也改不成别的房间重放。
#      · `/native/lk/*` —— 信令本来就设计成公开的，真正的门是那张 15 分钟 JWT，
#        而 JWT 只能从上面那条验过签的路换到。
#      · `/native/admin*` 和 `/native/api/admin/*` —— **没有补偿，直接 404**。
#        管理台自己不带鉴权，给它配一把 native 密钥等于把 deleteRoom 开到公网。
#        原生 app 也不需要它。Caddy 挡一道、路由自己再拒一道（`isNativeEntry`
#        直接回 404），两道都留着。
#    豁免的**只有 `/native/` 这个前缀本身**：`/api/*`、`/lk/*`、`/admin*` 的裸
#    路径仍然全在 IAP 后面。验证方法是对裸路径伪造一个 `X-CC-Entry: native`
#    再请求一次 —— 应该还是被弹到 Google 登录页（2026-09-13 实测如此）。
#
#    ⚠️ **开这个口子前，把「前缀剥掉之后能打到哪些路由」一条条数一遍。**
#    我第一次加 `/native/*` 时漏了 `/api/admin/*`，公网上多了一个无鉴权的
#    deleteRoom 接口约一小时。危险的不是这条豁免规则，是 `handle_path` 剥前缀
#    这个行为 —— 它让一条看起来很窄的新路径，等价于把**整棵路由树**再暴露一遍。
#
#    url-map 那一侧要加的东西（`gcloud compute url-maps export/import`）：
#      pathMatchers[name=<挂着 IAP 后端的那个>].pathRules 里追加
#        - paths: ['/native/*']
#          service: <不开 IAP 的那个 backendService>
#    改完**要等几分钟**：url-map 是**逐台 GFE** 生效的，传播窗口里同一个 URL
#    两次请求会打到新旧两套配置上，看起来像「时灵时不灵的 bug」。
#    别去 debug 逻辑，轮询到连续几轮结果一致为止。
# 2. 健康检查来源网段（35.191.0.0/16、130.211.0.0/22）的防火墙规则如果放的是
#    「全部 TCP / 全部协议」，那 7880 对这些网段是**直接可达的**，绕过 Caddy。
#    按需收窄到 tcp:80。
# 3. 关掉 443 时：摘掉实例的 https-server tag（**规则本身还在**，只是不再套用），
#    并删掉旧 signaling 域名的 DNS A 记录。
# 4. rtc.tcp_port（7881）是 UDP 被封时的兜底。摘掉对应 tag 等于关掉兜底，
#    但 SFU 仍会 advertise 这个 candidate —— 在封 UDP 的网络下客户端会直接失败
#    而不是降级。要么两边都留，要么两边都去掉。
