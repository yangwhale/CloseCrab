---
repo: {org}/{repo}
source: https://codewiki.google/github.com/{org}/{repo}
fetched_at: YYYY-MM-DD HH:MM HKT
codewiki_commit: {SHA from page footer, or "unknown"}
---

# {repo} — Code Wiki Recon

> **Authority warning**: This summary is auto-generated by Gemini from public README/code. It is reliable for **architecture and module boundaries**, unreliable for **runtime behavior, optimization, or non-obvious design decisions**. Verify by reading actual code before acting on details.

## TOC (high-level structure)

Render the TOC tree pulled in Step 2. Indent by `level`:

- Top-level section 1
  - Subsection 1.1
  - Subsection 1.2
- Top-level section 2
  - ...

## Architecture overview

Paste the first 8000 chars of the main page body (Step 3). Trim trailing whitespace. Do NOT edit/summarize — keep verbatim so the source is traceable.

Optionally embed a screenshot if one was saved successfully (path varies by MCP):

`![overview](path/to/codewiki-{org}-{repo}-overview.png)`

## Section summaries

For each level-1 TOC entry, one subsection with the first ~500 chars from Step 4:

### {Section title}

{500-char snippet}

### {Next section title}

{500-char snippet}

...

## Useful entry points

If Code Wiki linked to specific files in its overview/summaries, list the top 5-10 with their GitHub commit-pinned links. Format:

- `{path}#L{line}` — {one-line purpose}, [source]({github_commit_pinned_url})

This is the most reusable artifact — it's the "where do I start reading" cheatsheet.

## Notes for future Claude turns

(Optional — only fill in if you noticed something the user will care about, e.g., "This repo uses a non-standard layout: source under `src/maxtext/` not `maxtext/`", or "Watch out: the `inference/` and `inference_microbenchmarks/` directories are unrelated".)
