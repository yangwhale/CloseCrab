# Skill Allowlist — deploy 时只 link/install 这里列出的 skill
#
# 一行一个 skill 名 (kebab-case). # 开头的行是注释.
# 不在这里的 skill 会被 dispatch-bot.sh / install-private-skills.sh / deploy.sh 跳过.
#
# 源码完整保留在 ~/CloseCrab/skills/ (public) 和 $PRIVATE_SKILLS_DIR (private, 可选).
# 临时需要某个 archive 的 skill, 在这里加一行 + 跑 deploy 即可恢复.
#
# 2026-05-22 初版 23 个 active (从 50 砍下来, 28 个 archive 到 ~/.claude/skills-archive/).
# 见 ~/.claude/skills-archive/README.md 了解 archive 分档.

# === A 档: 核心日常 (8) ===
agent-teams
chat-style
evolution
notify
session-handoff
smoke-test
tts-generator
wiki

# === B 档: 周/月使用 (15) ===
bot-config
browser-cli
feishu-mail
weather-forecast
imagen-generator
music-generator
page-style
skill-creator
live-canvas
cc-pages-backup
wechat-reader

# ── NVIDIA GPU Skills (Megatron-Core / NeMo / TileGym) ──
# 默认不装。需要时: deploy.sh --nvidia-skills 或手动 link
# 存放在 skills-nvidia/ 目录，不在 skills/ 里
# mcore-create-issue
# mcore-linting-and-formatting
# mcore-run-on-slurm
# mcore-split-pr
# mcore-testing
# nemo-mbridge-* (17 skills)
# nemo-automodel-* (4 skills)
# nemo-rl-* (4 skills) + launch-nemo-rl
# tilegym-* (7 skills)
# === C 档: 后续追加 ===
# 这几条原本直接跟在上面 NVIDIA 注释块后面，容易被误读成"也是注释掉的"，
# 2026-08-09 起单独立一节。
hk-share-award-tax-dipn38
deck-builder
nvl72-qa

# === 2026-08-09 补：使用频次不低但一直没进 allowlist（等于用得着却没装）===
hk-bus
feishu-user-msg

# === 2026-08-09 精简：以下按「SKILL.md 实际被加载次数」判定为低频，移出 allowlist ===
# 源码都还在仓库里，需要时加回一行 + 跑 deploy 即可。
# code-wiki-recon        — 低频，需要时加回
# fireworks-tech-graph   — 低频，需要时加回
# frontend-slides        — 被 deck-builder 取代
# gemini-cli             — 有 Gemini worker，不需要再从 skill 里调
# go-eat                 — 食堂菜单，几乎不用
# internal-doc-site      — 低频，需要时加回
# memory-gc              — 2026-08-09 我基于坏指标加的，实测低频，撤回
# memory-lint            — 同上，撤回
# vector                 — Salesforce CRM，几乎不用

# === 只存在于私有 skill 目录的条目 ===
# 这几个在 public 仓库里没有对应目录，只有配了 PRIVATE_SKILLS_DIR 的机器才装得上。
# 以前默认注释掉，因为放开对外部使用者是「静默无效」—— deploy 找不到目录直接跳过、
# 不报错。2026-08-10 给 deploy.sh 加了缺源告警之后不再静默：没配私有目录的机器
# 会看到一行「allowlist 有 N 条在本机找不到源」并列出名字。所以可以正常列出来了。
google-internal-mcp
tpu-v7-cluster
# customer-qa-tracker    — 近 7 天 0 次，最后一次 2026-06-24
# ant-tpu-sync           — 近 7 天 0 次，最后一次 2026-07-24

# === 2026-08-10 复审：改用「排除自匹配 + 按条目时间戳」的访问统计重判 ===
# 上一版（08-09）用的是名字提及次数，被我自己列举 skill 的命令污染，指标是坏的。
# 这次统计路径访问 skills/<name>/，排除当前会话，并从 JSONL 每条 entry 自带的
# timestamp 取时间分布 —— 总数和近况经常讲两个不同的故事：
#   chrome-browser 跨月 4 次，但 browser-cli 8/5 上线后同期 102 次 → 取代是真的
#   research 共 9 次，全部集中在 6/29-6/30 → 四十天没用了
chrome-browser
multimodal-explainer
glinux-tools
